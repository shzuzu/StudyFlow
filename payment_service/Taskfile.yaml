version: "3"

tasks:
  generate:
    aliases:
      - gen
    desc: "Generate Go code from proto files"
    cmds:
      - protoc -I proto proto/payment_service.proto --go_out=./api/gen/ --go_opt=paths=source_relative --go-grpc_out=./api/gen/ --go-grpc_opt=paths=source_relative
